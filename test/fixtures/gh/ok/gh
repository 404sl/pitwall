#!/bin/sh
if [ -n "$GH_LOG" ]; then
  echo "$*" >> "$GH_LOG"
fi
case "$1 $2" in
  "auth status") echo "Logged in to github.com account nobody" ;;
  "pr list") cat "${GH_OUTPUT:?GH_OUTPUT is not set}/list.json" ;;
  *) echo "unknown command: gh $*" >&2; exit 1 ;;
esac
