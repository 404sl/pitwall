#!/bin/sh
out="${GH_OUTPUT:?GH_OUTPUT is not set}"
if [ -n "$GH_LOG" ]; then
  echo "$*" >> "$GH_LOG"
fi
case "$1 $2" in
  "pr list") cat "$out/list.json" ;;
  *) echo "unknown command: gh $*" >&2; exit 1 ;;
esac
